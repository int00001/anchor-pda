[features]
seeds = false
skip-lint = false
[programs.devnet]
anchor_pda = ""

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = ""

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
